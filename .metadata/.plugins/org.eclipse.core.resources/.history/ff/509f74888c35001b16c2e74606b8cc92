/* Quando eu declaro uma classe, na verdade eu estou declarando um novo tipo de dado (estrutura)
 * Neste caso, o tipo de CARRO contém propriedades (atributos) que são: Marca, modelo, ano, cor e preço.
 */

public class Carro {
	
	String marca;
	String modelo;
	String cor;
	int    ano;
	double preco;	
	
	
	//função IMPRIMIR
	void imprimir(){
		System.out.println("");
		System.out.println("--------CARRO---------");
		System.out.println("Marca: "+marca+"/"+modelo);
		System.out.println("Ano: "+ano+ " / Cor: "+cor);
		System.out.println("Preço: R$ "+preco);
	}
	
	/*método calculo IPVA
	 * metodo é um trecho de codigo, como todo trecho codigo ele segue o padrão E-> P-> S
	 * a declaração do metodo é feita da seguinte forma:
	 * 
	 * tipo Saida  nomeMetodo (lista de paramentos de entrada){
	 * .... logica (processamento)
	 * }
	 * 
   */
	double calcularIPVA() {
		double valorIpva;
		if (ano < 2000) {
			valorIpva = 0;
		}
		else 
			valorIpva = preco * 0.04;
		return valorIpva;
	}
}
